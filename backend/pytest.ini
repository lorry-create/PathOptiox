[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
asyncio_mode = auto
addopts = -v --tb=short --strict-markers
markers =
    integration: API 集成测试（需要数据库）
    unit: 纯单元测试（无外部依赖）
